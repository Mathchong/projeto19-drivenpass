import { Router } from 'express';

const credentialsRouter = Router();

credentialsRouter.post('/', () => { })
credentialsRouter.get('/', () => { })
credentialsRouter.get('/:id', () => { })
credentialsRouter.delete('/:id', () => { })

export default credentialsRouter;