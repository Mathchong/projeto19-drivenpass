import { Router } from 'express';

const wifisRouter = Router();

wifisRouter.post('/', () => { })
wifisRouter.get('/', () => { })
wifisRouter.get('/:id', () => { })
wifisRouter.delete('/:id', () => { })

export default wifisRouter;