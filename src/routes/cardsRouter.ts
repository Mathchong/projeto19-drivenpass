import { Router } from 'express';

const cardsRouter = Router();

cardsRouter.post('/', () => { })
cardsRouter.get('/', () => { })
cardsRouter.get('/:id', () => { })
cardsRouter.delete('/:id', () => { })

export default cardsRouter;